<div class="min-h-screen bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900 p-6">
  <div class="max-w-4xl mx-auto">
    <!-- ヘッダー -->
    <div class="text-center mb-8">
      <h1 class="text-4xl font-bold text-white mb-2">🐇 RabbitHole 診断結果</h1>
      <p class="text-purple-200">
        <%= @result.fortune_type %> - <%= @result.created_at.strftime("%Y年%m月%d日 %H:%M") %>
      </p>
    </div>

    <!-- 基本情報 -->
    <div class="bg-white/10 backdrop-blur-md rounded-xl p-6 mb-6">
      <h2 class="text-2xl font-bold text-white mb-4">📊 基本情報</h2>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-white">
        <div>
          <p class="text-purple-200">生年月日</p>
          <p class="font-bold"><%= @master.year %>年<%= @master.month %>月<%= @master.day %>日</p>
        </div>
        <div>
          <p class="text-purple-200">性別</p>
          <p class="font-bold"><%= @master.sex %></p>
        </div>
        <div>
          <p class="text-purple-200">天中</p>
          <p class="font-bold"><%= @master.tenchu_1 %><%= @master.tenchu_2 %></p>
        </div>
        <div>
          <p class="text-purple-200">エネルギー系</p>
          <p class="font-bold"><%= @master.energy_kei %></p>
        </div>
      </div>
    </div>

    <!-- 命式詳細 -->
    <div class="bg-white/10 backdrop-blur-md rounded-xl p-6 mb-6">
      <h2 class="text-2xl font-bold text-white mb-4">🔮 命式詳細</h2>
      
      <!-- 年月日柱 -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        
        <!-- 日柱 -->
        <div class="bg-white/5 rounded-lg p-4">
          <h3 class="text-lg font-bold text-blue-300 mb-3">日柱</h3>
          <div class="space-y-2 text-white text-sm">
            <p><span class="text-purple-200">通変星:</span> <%= tsuhen_icon_tag(@master.tsuhen_day) || content_tag(:span, @master.tsuhen_day) %></p>
            <p><span class="text-purple-200">蔵干通変星:</span> <%= tsuhen_icon_tag(@master.ztsuhen_day) || content_tag(:span, @master.ztsuhen_day) %></p>
            <p><span class="text-purple-200">十二運:</span> <%= unsei_icon_tag(@master.unsei12_day) || content_tag(:span, @master.unsei12_day) %></p>
          </div>
        </div>
        
        <!-- 月柱 -->
        <div class="bg-white/5 rounded-lg p-4">
          <h3 class="text-lg font-bold text-green-300 mb-3">月柱</h3>
          <div class="space-y-2 text-white text-sm">
            <p><span class="text-purple-200">通変星:</span> <%= tsuhen_icon_tag(@master.tsuhen_month) || content_tag(:span, @master.tsuhen_month) %></p>
            <p><span class="text-purple-200">蔵干通変星:</span> <%= tsuhen_icon_tag(@master.ztsuhen_month) || content_tag(:span, @master.ztsuhen_month) %></p>
            <p><span class="text-purple-200">十二運:</span> <%= unsei_icon_tag(@master.unsei12_month) || content_tag(:span, @master.unsei12_month) %></p>
          </div>
        </div>

        <!-- 年柱 -->
        <div class="bg-white/5 rounded-lg p-4">
          <h3 class="text-lg font-bold text-yellow-300 mb-3">年柱</h3>
          <div class="space-y-2 text-white text-sm">
            <p><span class="text-purple-200">通変星:</span> <%= tsuhen_icon_tag(@master.tsuhen_year) || content_tag(:span, @master.tsuhen_year) %></p>
            <p><span class="text-purple-200">蔵干通変星:</span> <%= tsuhen_icon_tag(@master.ztsuhen_year) || content_tag(:span, @master.ztsuhen_year) %></p>
            <p><span class="text-purple-200">十二運:</span> <%= unsei_icon_tag(@master.unsei12_year) || content_tag(:span, @master.unsei12_year) %></p>
          </div>
        </div>
        
      </div>
    </div>

    <!-- AI診断結果 -->
    <div class="bg-white/10 backdrop-blur-md rounded-xl p-6 mb-6">
      <h2 class="text-2xl font-bold text-white mb-4">🤖 AI診断結果</h2>
      
      <!-- 入力メモ -->
      <% if @result.memo.present? %>
      <div class="bg-white/5 rounded-lg p-4 mb-4">
        <h3 class="text-lg font-bold text-orange-300 mb-2">📝 相談内容</h3>
        <p class="text-white"><%= @result.memo %></p>
      </div>
      <% end %>
      
      <!-- 性格分析 -->
      <div class="bg-white/5 rounded-lg p-4 mb-4">
        <h3 class="text-lg font-bold text-pink-300 mb-2">✨ 性格・特性</h3>
        <p class="text-white leading-relaxed"><%= @result.ai_summary %></p>
      </div>
      
      <!-- アドバイス -->
      <div class="bg-white/5 rounded-lg p-4">
        <h3 class="text-lg font-bold text-cyan-300 mb-2">💡 アドバイス</h3>
        <p class="text-white leading-relaxed"><%= @result.ai_advice %></p>
      </div>
    </div>

    <!-- 注意事項 -->
    <div class="bg-red-500/20 backdrop-blur-md rounded-xl p-6 border border-red-500/30">
      <h2 class="text-xl font-bold text-red-300 mb-2">⚠️ 重要な注意事項</h2>
      <div class="text-red-100 text-sm space-y-1">
        <p>• この診断結果は<strong>5分間限定</strong>で公開されています</p>
        <p>• 5分経過後は、この端末（セッション）でのみ再閲覧可能です</p>
        <p>• 必要であれば今すぐスクリーンショットを保存してください</p>
        <p>• この結果は自動的に削除される場合があります</p>
      </div>
    </div>

    <!-- フッター -->
    <div class="text-center mt-8">
      <p class="text-purple-300 text-sm">
        Powered by RabbitHole × AI診断
      </p>
    </div>
  </div>
</div> 