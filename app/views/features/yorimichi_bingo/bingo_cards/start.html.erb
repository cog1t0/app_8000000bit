<% content_for :title, "よりみちびんご" %>

<div class="w-full">
  <div class="rounded-3xl bg-gradient-to-br from-amber-500 via-pink-500 to-indigo-600 px-6 py-10 shadow-2xl">
    <div class="flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.2em] text-white/80">Yorimichi Bingo</p>
        <h1 class="mt-2 text-4xl font-extrabold text-white md:text-5xl">いつもの毎日に、ひとつ寄り道を。</h1>
        <p class="mt-4 max-w-2xl text-lg text-white/90">
          30日間の小さな冒険を友だちや家族とシェアするビンゴカードを作れます。
          タイトルと日数を決めたら、さっそく「あたらしいびんごを作る」を押してみてください。
        </p>
        <div class="mt-6 flex flex-wrap gap-3 text-sm text-white/80">
          <span class="rounded-full bg-white/15 px-4 py-2">共有URLで同じカードにアクセス</span>
          <span class="rounded-full bg-white/15 px-4 py-2">5x5のよりみちタスク</span>
          <span class="rounded-full bg-white/15 px-4 py-2">期限が来たら閲覧のみ</span>
        </div>
      </div>
      <div class="rounded-2xl bg-white/15 p-4 text-white shadow-xl backdrop-blur">
        <p class="text-sm font-semibold text-white/70">MVP scope</p>
        <ul class="mt-3 space-y-2 text-sm leading-relaxed">
          <li>・5x5マスをタップで更新</li>
          <li>・期限が切れたらロック</li>
          <li>・リセットで新しいカードを発行</li>
        </ul>
      </div>
    </div>
  </div>

  <div class="mt-10 grid gap-8 md:grid-cols-2">
    <div class="rounded-2xl bg-white shadow-2xl ring-1 ring-slate-200">
      <div class="border-b border-slate-100 px-6 py-4">
        <h2 class="text-lg font-semibold text-slate-800">びんごカードを作る</h2>
      </div>
      <div class="px-6 py-5">
        <turbo-frame id="flash">
          <%= render "features/yorimichi_bingo/bingo_cards/flash" %>
        </turbo-frame>

        <%= form_with url: yorimichi_bingo_create_path, scope: :bingo_card, class: "space-y-4" do |f| %>
          <div>
            <%= f.label :title, "カードのタイトル", class: "block text-sm font-medium text-slate-700" %>
            <%= f.text_field :title,
              value: @bingo_card.title,
              placeholder: "いつもとちがう30日",
              class: "mt-1 w-full rounded-xl border border-slate-200 px-3 py-2 text-slate-900 shadow-sm focus:border-amber-400 focus:outline-none focus:ring focus:ring-amber-200" %>
            <p class="mt-1 text-xs text-slate-500">家族や友だちと共有するテーマを書いてください。</p>
          </div>

          <div>
            <%= f.label :duration_days, "期限（日数）", class: "block text-sm font-medium text-slate-700" %>
            <%= f.number_field :duration_days,
              value: @duration_days,
              min: 1,
              class: "mt-1 w-32 rounded-xl border border-slate-200 px-3 py-2 text-slate-900 shadow-sm focus:border-amber-400 focus:outline-none focus:ring focus:ring-amber-200" %>
            <p class="mt-1 text-xs text-slate-500">今日から何日後まで遊べるかを決めます（デフォルト30日）。</p>
          </div>

          <div class="pt-2">
            <%= f.submit "あたらしいびんごを作る", class: "w-full rounded-xl bg-gradient-to-r from-amber-400 via-rose-400 to-indigo-500 px-4 py-3 text-center text-base font-semibold shadow-lg transition hover:scale-[1.01] hover:shadow-xl" %>
          </div>
        <% end %>
      </div>
    </div>

    <div class="rounded-2xl border border-dashed border-amber-200 bg-amber-50/60 p-6 shadow-inner">
      <p class="text-sm font-semibold uppercase tracking-[0.2em] text-amber-500">Preview</p>
      <h3 class="mt-2 text-2xl font-bold text-amber-800">5x5 びんご盤</h3>
      <p class="mt-1 text-sm text-amber-700">ランダムではなく、日常に寄り道できるタスクが25個並びます。</p>

      <div class="mt-5 grid grid-cols-5 gap-2">
        <% Features::YorimichiBingo::BoardBuilder::DEFAULT_ITEMS.first(25).each_with_index do |title, idx| %>
          <div class="h-16 rounded-xl bg-white px-2 py-2 text-center text-xs font-semibold text-amber-800 shadow-sm ring-1 ring-amber-200/70">
            <div class="text-[10px] text-amber-500">#<%= idx + 1 %></div>
            <div class="line-clamp-2 leading-tight"><%= title %></div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
