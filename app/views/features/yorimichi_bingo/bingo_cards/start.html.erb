<% content_for :title, "よりみちびんご" %>

<div class="w-full">
  <div class="rounded-3xl bg-gradient-to-br from-amber-500 via-pink-500 to-indigo-600 px-6 py-12 shadow-2xl">
    <div class="space-y-6 text-white md:space-y-8">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.2em] text-white/80">Yorimichi Bingo</p>
        <h1 class="mt-2 text-4xl font-extrabold md:text-5xl">いつもの毎日に、ひとつ寄り道を。</h1>
        <p class="mt-4 max-w-3xl text-lg text-white/90">
          スタートボタンを押すと、カテゴリーを選べる編集画面へ進みます。3〜8個のテーマを選ぶだけで、25マスのよりみちタスクが自動で並びます。
        </p>
      </div>
      <div class="flex flex-wrap gap-3 text-sm text-white/80">
        <span class="rounded-full bg-white/15 px-4 py-2">共有URLで同じカードにアクセス</span>
        <span class="rounded-full bg-white/15 px-4 py-2">3カテゴリ以上を組み合わせて生成</span>
        <span class="rounded-full bg-white/15 px-4 py-2">期限が来たら閲覧のみ</span>
      </div>
      <div class="rounded-2xl bg-black/20 p-6 backdrop-blur">
        <turbo-frame id="flash">
          <%= render "features/yorimichi_bingo/bingo_cards/flash" %>
        </turbo-frame>
        <% if defined?(@error_messages) && @error_messages.present? %>
          <ul class="mb-4 list-disc pl-5 text-sm text-rose-100">
            <% @error_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        <% end %>
        <div class="flex flex-col gap-3 md:flex-row md:items-center">
          <%= button_to "スタート", yorimichi_bingo_create_path,
              method: :post,
              class: "w-full rounded-2xl bg-white px-5 py-4 text-center text-lg font-semibold text-amber-600 shadow-xl transition hover:scale-[1.01] hover:bg-white/90",
              form: { data: { turbo: false } } %>
          <p class="text-sm text-white/80 md:w-1/2">ボタンを押すと初回だけ編集できるページに移動します。カテゴリを選んでカードを完成させたら、共有URLが表示されます。</p>
        </div>
      </div>
    </div>
  </div>
</div>
