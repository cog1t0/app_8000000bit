<% content_for :title, "#{@bingo_card.title} | よりみちびんご" %>

<div class="w-full space-y-6">
  <turbo-frame id="flash">
    <%= render "features/yorimichi_bingo/bingo_cards/flash" %>
  </turbo-frame>

  <div class="rounded-3xl bg-gradient-to-br from-amber-500 via-rose-500 to-indigo-600 px-6 py-6 text-white shadow-2xl">
    <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.25em] text-white/70">Yorimichi Bingo</p>
        <h1 class="text-3xl font-extrabold md:text-4xl"><%= @bingo_card.title %></h1>
        <p class="mt-2 max-w-2xl text-white/90">みんなで同じURLにアクセスして、達成したマスをタップするとすぐに反映されます。</p>
      </div>
      <div class="flex flex-wrap items-center gap-3">
        <%= button_to "あたらしいびんごを作る", yorimichi_bingo_reset_path(token: @bingo_card.token),
            method: :post,
            form: { data: { turbo_confirm: "新しいカードを作りますか？今のカードはそのまま残ります。" } },
            class: "rounded-xl bg-white/90 px-4 py-3 text-sm font-semibold text-amber-700 shadow-lg transition hover:scale-[1.01] hover:bg-white" %>
        <a href="#bingo-board" class="rounded-xl bg-black/25 px-4 py-3 text-sm font-semibold text-white shadow hover:bg-black/35">ビンゴ盤へジャンプ</a>
      </div>
    </div>

    <div class="mt-5 grid gap-3 md:grid-cols-[2fr,1fr]">
      <div class="rounded-2xl bg-white/15 p-4 backdrop-blur">
        <p class="text-xs font-semibold uppercase tracking-[0.2em] text-white/80">共有URL</p>
        <div class="mt-2 flex flex-col gap-2 md:flex-row md:items-center">
          <input type="text" readonly value="<%= @share_url %>" class="w-full rounded-xl border border-white/30 bg-black/10 px-3 py-2 text-sm font-semibold text-white placeholder:text-white/60 focus:border-white focus:outline-none focus:ring-2 focus:ring-white/70">
          <p class="text-[11px] text-white/80 md:w-48">URLをコピーして家族や友だちに送ってね。</p>
        </div>
      </div>
      <div class="rounded-2xl bg-white/15 p-4 text-sm font-semibold backdrop-blur">
        <p class="text-xs uppercase tracking-[0.2em] text-white/70">更新ルール</p>
        <ul class="mt-2 space-y-1 text-white/90">
          <li>・期限を過ぎると更新できなくなります</li>
          <li>・マスをタップすると即座に反映されます</li>
          <li>・リセットすると新しいtokenで再スタート</li>
        </ul>
      </div>
    </div>
  </div>

  <%= render "features/yorimichi_bingo/bingo_cards/status", bingo_card: @bingo_card, checker: @checker %>

  <div id="bingo-board">
    <%= render "features/yorimichi_bingo/bingo_cards/grid", bingo_card: @bingo_card %>
  </div>
</div>
